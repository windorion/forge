# 12a · Quick Capture — 2026-07-19 · SHA d979852

Evidence: `20260719T173829Z_d979852_window3.png` (panel).

Built from Missing: QuickCaptureController registers a Carbon
RegisterEventHotKey global hotkey (⌥Space default, user-configurable via
the forge.quickCaptureHotkey default; Carbon needs no Accessibility
permission) toggling a borderless floating NSPanel (statusBar level, all
Spaces) hosting QuickCaptureView: logo + focused 15px input + ESC cap,
REPO chips from the real registered repositories (accent selection, ⇥
hint), an honest AI hint row (matches the local template library on
keywords, otherwise states the real plan-gate guarantee), options row
with the real model/effort/guardrails values + "⌘, change defaults",
and PLAN ONLY ⌥↵ / ▸ PLAN & RUN ↵ actions creating real tasks (PLAN &
RUN additionally fronts the main window on the new task).

Recorded gaps: the mockup's file-match intelligence ("matched:
auth.spec.ts — 3 flaky runs this week") needs repository indexing (P3);
the template-match hint is the honest current equivalent. Hotkey pressing
awaits a human keypress pass (registration verified in code; panel driven
via the quickCapture debug spec). Verified (with notes).
