# 3a · Settings — Model

- Date: 2026-07-19 · SHA f06966b (+fixes)
- Evidence: `20260719T062452Z_f06966b_Forge-Settings.png` (final)

Deltas fixed: reasoning-effort labels LOW/STANDARD/MAX with the mockup's
default note; budget helper now real "$X.XX used this month · pauses
(never aborts) at cap" and the bar fill uses the real spend/cap fraction;
footer replaced with real usage stats ("this month: N runs · avg $X/run ·
cheaper than the coffee…"); SAVE MODEL retained (explicit save is the
guardrail pattern).

Intentional deviations: provider cards list the runtime's real providers
(LOCAL PROVIDER / LOCAL-DETERMINISTIC-V0), not the mockup's illustrative
Claude lineup; MODEL NAME + ADD API KEY row remains (API keys live on the
30a page in the app's 7-item sidebar).

Sidebar ruling: the handoff draws three different sidebars across settings
screens (5, 6, and 7 items); `30a`'s 7-item version matches the app
exactly and is treated as the design's latest evolution. Recorded once
here for all settings pages. Verified (with notes).
