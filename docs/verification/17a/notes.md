# 17a · No Repository

- Date: 2026-07-19 · SHA 30b41e9 (+fixes)
- Evidence: `20260719T114333Z_30b41e9_Forge.png` (final)

Fixed: subtitle no longer truncates ("…Forge plans before it touches
anything, so nothing happens without your OK." now wraps via explicit
lineLimit(nil) + fixedSize). Matches: dashed logo placeholder, headline,
CONNECT A REPO (accent) / TRY THE DEMO REPO, 01→02→03 step strip, demo
sandbox footnote. Both actions are real (folder picker persists and
starts the runtime; demo creates the sandbox repo). Verified.
